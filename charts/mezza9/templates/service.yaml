apiVersion: v1
kind: Service
metadata:
  name: {{ include "mezza9.fullname" . }}
  labels:
    {{- include "mezza9.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mezza9.selectorLabels" . | nindent 4 }}
